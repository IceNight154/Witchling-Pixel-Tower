package com.shatteredpixel.shatteredpixeldungeon.actors.hero;

/**
 * Applies/removes stance-specific passive bonuses via attaching/removing light buffs.
 * This is intentionally minimal so it can compile even if you move things around.
 */
public class ElementBonuses {
    public static void apply(Hero h, ElementStance s){
        // TODO: Attach small marker buffs or set flags on hero for your combat pipeline.
        // Keep minimal here to avoid compile coupling.
    }
}
